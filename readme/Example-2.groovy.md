```groovy
pipeline {  // определяет все содержимое конвеера
    agent any  // говорит что может запукаться на любомо агенте
    stages { // описывает  этапы или стадии выполнення
        stage('Build') { // этап выполнения
            steps { // шаги выполнения в stage 
                echo 'Hello Pipeline' // что выводит
            }
        }
    }
}
```